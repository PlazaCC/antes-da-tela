// Placeholder MSW handlers file. Add request handlers here as needed.
// Example:
// import { rest } from 'msw'
// export const handlers = [rest.get('/api/user', (req, res, ctx) => res(ctx.json({ id: 1 })))]

export const handlers = []
